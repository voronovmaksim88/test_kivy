#:kivy 2.3.1
Label:  # Это корневой виджет. Kivy заменит им то, что возвращает метод build()
    text: 'А это текст из .kv файла!'  # Устанавливаем свойство text
    font_size: '50sp'  # Устанавливаем размер шрифта (sp - scalable pixels)
    color: 1, 0.5, 0, 1  # Устанавливаем цвет текста (RGBA, от 0 до 1)